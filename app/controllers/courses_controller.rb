class CoursesController < ApplicationController
  def index
    # want to display the Major, Requirements, and Classes in the view
    # @major = 
    # @requirements = filter requirements for specific majorminor
    # @schedule = Schedule.find(params[:uni]) filter Schedules by uni?
  end
end
